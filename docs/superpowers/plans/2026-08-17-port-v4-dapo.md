# Port protocole v4 (qwen3-4b-base-dapo-v4) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Rendre le mineur conforme au protocole v4 (profil upstream
`qwen3-4b-base-dapo-v4`, branche `feat/qwen3-base-dapo-v4-profile` @ `8c38992`)
derrière le flag `RELIQUARY_PROTOCOL_VERSION=4`, avec un comportement v3
**byte-identique** quand le flag est absent (défaut 3).

**Architecture:** Toutes les valeurs v4 sont dérivées dans
`reliquary/constants.py` d'un nouveau `PROTOCOL_VERSION = protocol_version()`
(pattern upstream `X if PROTOCOL_VERSION >= 4 else Y`). Les modules
consommateurs qui doivent être testables sans reload font un import paresseux
(`from reliquary.constants import X` DANS la fonction), exactement comme
upstream. Le jour J, la bascule = flag de lancement, pas un changement de code.

**Tech Stack:** Python 3, pytest (CPU-only sur la dev box — pas de GPU, pas de
vLLM), git worktree `/root/subnet81/.worktrees/miner-priv-port-v4-dapo`,
branche `feat/port-v4-dapo`.

**Spec:** le diff upstream `origin/main...origin/feat/qwen3-base-dapo-v4-profile`
dans `/root/subnet81/reliquary` (clone read-only). Résumé du contrat v4 ci-dessous.

## Contrat v4 (vérifié dans le code upstream @ 8c38992)

| Valeur | v3 (live) | v4 |
|---|---|---|
| model_id | Qwen/Qwen3.5-4B | `Qwen/Qwen3-4B-Base` @ `906bfd4b4dc7f14ee4320094d8b41684abff8539` |
| protocol_version | 3 | 4 (domaine forced-seed `reliquary-forced-seed-v4`, dérivé) |
| rollouts (M) | 8 | 16 |
| sampling | T 0.6 / top_p 0.95 / top_k 20 | T 1.0 / top_p 1.0 / **top_k 0** (support complet) |
| BFT | math 15616/512 | **aucun** (les 2 envs `bft=None`) ; toute claim `forced` ⇒ rejet fail-closed |
| max_new_tokens | 16384 | 8192 (les 2 envs) ; tiebreak token_cap 8192, bucket 50 |
| fenêtre | 300 s | 150 s (côté mineur : suivi automatique des flips drand — rien à porter) |
| prompt encoding | chat template | **brut**, `add_special_tokens=False`, jamais de template (`RAW_COMPLETION_PROMPTS`) |
| OMI manifest | tous les shards | **`train-*` seulement** (`OMI_TRAIN_SHARDS_ONLY`) — len(env) = consensus prompt-range |
| zone gate σ | 0.43 / 0.33 | **0.24 / 0.22** (admet k∈[1,15] sur M=16) |
| soumissions/hotkey/fenêtre | 8 | **32** (2·B_BATCH, B_BATCH=16) |
| MIN_EOS_PROBABILITY | 0.01 | 0.001 |
| SAMPLING_MEDIAN_LOW_MAX / LOW_Q10_MAX | 0.30 / 0.025 | 0.05 / 0.0002 |
| forced-seed floors | 0.80 groupe / 0.75 rollout | 0.80 groupe (inchangé) / **0.70** rollout |
| grader math | boxed + trailing-number | + ligne `Answer:` + strip `\( \) \[ \]` (gaté RAW_COMPLETION) |

Non-applicable à notre fork : `seed_consistency_diagnostics` dense
(fb283c7 — fonction absente chez nous), tout `validator/training.py`
(PPO/DAPO côté validateur), `_use_8bit_optimizer`.

Déjà v4-safe (vérifié, ne pas re-toucher) : `warp()`, `warp_fast()`,
`force_rows_batched()` et `force_rows_batched_fast()` gardent tous
`if top_k and top_k > 0` / `if top_p and top_p < 1.0` → softmax identité à
T=1.0/1.0/0. `forced_seed_domain(4)` produit déjà le bon domaine.

## Global Constraints

- **Byte-identité v3** : sans `RELIQUARY_PROTOCOL_VERSION=4` dans l'env, AUCUNE
  valeur effective ne change. Chaque tâche a un test « défaut = v3 ».
- **Parité byte-exacte** : le code porté copie l'upstream à l'identique (mêmes
  regex, mêmes ordres d'opérations) — pas de « équivalent ».
- Pas d'install de reliquary sur la dev box ; tests CPU-only via
  `PYTHONPATH=. python3 -m pytest`.
- Baseline connue : 7 fails pré-existants = fixtures v2 périmées
  (`test_forced_seed_v2_port.py`, `test_forced_seed_constants.py`,
  `test_wire_v2_port.py` assertent v2 alors que l'arbre émet 3). Ne pas les
  « réparer » dans ce chantier ; ne pas en ajouter.
- Commits sur la branche `feat/port-v4-dapo` uniquement (l'arbre prod
  `/root/subnet81/reliquary-miner-priv` reste intouché).
- Helper de test partagé (défini Task 1, réutilisé partout) :

```python
# tests/v4helpers.py
import importlib


def reload_constants(monkeypatch, version=None):
    """Recharge reliquary.constants avec RELIQUARY_PROTOCOL_VERSION=version.

    version=None → env nettoyé (défaut live v3). Rendre le module rechargé.
    Le caller DOIT re-appeler reload_constants(monkeypatch) en fin de test
    (fixture) pour restaurer l'état module global.
    """
    if version is None:
        monkeypatch.delenv("RELIQUARY_PROTOCOL_VERSION", raising=False)
    else:
        monkeypatch.setenv("RELIQUARY_PROTOCOL_VERSION", str(version))
    import reliquary.constants as c
    importlib.reload(c)
    return c
```

  et dans chaque fichier de test qui l'utilise :

```python
import pytest

@pytest.fixture(autouse=True)
def _restore_constants(monkeypatch):
    yield
    from tests.v4helpers import reload_constants
    reload_constants(monkeypatch)  # env nettoyé → re-import état v3
```

---

### Task 1: Constantes v4 dans `reliquary/constants.py`

**Files:**
- Modify: `reliquary/constants.py` (déplacer `protocol_version()` +
  `forced_seed_domain()` en tête ; gater les valeurs listées)
- Create: `tests/v4helpers.py` (code ci-dessus)
- Test: `tests/test_v4_constants.py`

**Interfaces:**
- Produces: `constants.PROTOCOL_VERSION: int`,
  `constants.RAW_COMPLETION_PROMPTS: bool`,
  `constants.OMI_TRAIN_SHARDS_ONLY: bool`, et les valeurs gatées du tableau
  spec. Tous les autres tasks consomment ces noms via import paresseux.

- [ ] **Step 1: Écrire les tests qui échouent**

```python
# tests/test_v4_constants.py
import pytest
from tests.v4helpers import reload_constants


@pytest.fixture(autouse=True)
def _restore_constants(monkeypatch):
    yield
    reload_constants(monkeypatch)


def test_default_is_v3_byte_identical(monkeypatch):
    c = reload_constants(monkeypatch)  # env nettoyé
    assert c.PROTOCOL_VERSION == 3
    assert c.M_ROLLOUTS == 8
    assert (c.T_PROTO, c.TOP_P_PROTO, c.TOP_K_PROTO) == (0.6, 0.95, 20)
    assert c.BFT_ENABLED is True
    assert c.MAX_NEW_TOKENS_PROTOCOL_CAP == 16384
    assert c.B_BATCH == 8
    assert c.MAX_SUBMISSIONS_PER_HOTKEY_PER_WINDOW == 8
    assert c.MIN_EOS_PROBABILITY == 0.01
    assert c.SAMPLING_MEDIAN_LOW_MAX == 0.30
    assert c.SAMPLING_LOW_Q10_MAX == 0.025
    assert c.FORCED_SEED_ROLLOUT_FLOOR == 0.75
    assert c.FORCED_SEED_CONSISTENCY_FLOOR == 0.80
    assert c.RAW_COMPLETION_PROMPTS is False
    assert c.OMI_TRAIN_SHARDS_ONLY is False
    assert c.FORCED_SEED_DOMAIN == "reliquary-forced-seed-v3"


def test_v4_values(monkeypatch):
    c = reload_constants(monkeypatch, 4)
    assert c.PROTOCOL_VERSION == 4
    assert c.M_ROLLOUTS == 16
    assert (c.T_PROTO, c.TOP_P_PROTO, c.TOP_K_PROTO) == (1.0, 1.0, 0)
    assert c.BFT_ENABLED is False
    assert c.MAX_NEW_TOKENS_PROTOCOL_CAP == 8192
    assert c.B_BATCH == 16
    assert c.MAX_SUBMISSIONS_PER_HOTKEY_PER_WINDOW == 32
    assert c.MIN_EOS_PROBABILITY == 0.001
    assert c.SAMPLING_MEDIAN_LOW_MAX == 0.05
    assert c.SAMPLING_LOW_Q10_MAX == 0.0002
    assert c.FORCED_SEED_ROLLOUT_FLOOR == 0.70
    assert c.FORCED_SEED_CONSISTENCY_FLOOR == 0.80  # inchangé en v4
    assert c.RAW_COMPLETION_PROMPTS is True
    assert c.OMI_TRAIN_SHARDS_ONLY is True
    assert c.FORCED_SEED_DOMAIN == "reliquary-forced-seed-v4"


def test_v4_cap_env_override_still_wins(monkeypatch):
    monkeypatch.setenv("RELIQUARY_MAX_NEW_TOKENS_PROTOCOL_CAP", "4096")
    c = reload_constants(monkeypatch, 4)
    assert c.MAX_NEW_TOKENS_PROTOCOL_CAP == 4096
    monkeypatch.delenv("RELIQUARY_MAX_NEW_TOKENS_PROTOCOL_CAP")


def test_v4_import_does_not_crash_on_bft_assert(monkeypatch):
    # v4 : cap 8192 < BFT 15616+512 — l'assert de cohérence BFT ne doit
    # s'appliquer que si BFT_ENABLED.
    c = reload_constants(monkeypatch, 4)
    assert c.MAX_NEW_TOKENS_PROTOCOL_CAP < (
        c.BFT_THINKING_BUDGET + c.BFT_ANSWER_BUDGET
    )
```

- [ ] **Step 2: Vérifier l'échec**

Run: `PYTHONPATH=. python3 -m pytest tests/test_v4_constants.py -q`
Expected: FAIL — `test_v4_values` (AttributeError `PROTOCOL_VERSION` ou
valeurs v3), et `test_v4_import_does_not_crash_on_bft_assert` en
AssertionError d'import (l'assert BFT du module saute avec cap 8192).

- [ ] **Step 3: Implémentation minimale dans `constants.py`**

1. Déplacer les définitions de `protocol_version()` et `forced_seed_domain()`
   (actuellement ~l.401-420) juste APRÈS le bloc d'imports/`_os` de tête, avant
   la première constante gatée (avant `MAX_NEW_TOKENS_PROTOCOL_CAP`, ~l.93).
   Ajouter dessous :

```python
PROTOCOL_VERSION = protocol_version()

# v4+ : protocole raw-completion — jamais de chat template (un repo "-Base"
# DÉCLARE le template famille sans l'avoir appris). Un seul switch pour
# encode_prompt ET le grader (parité upstream 8c38992).
RAW_COMPLETION_PROMPTS = PROTOCOL_VERSION >= 4

# v4+ : manifest OMI restreint aux shards canoniques train-* (les shards
# train_1M/2M/5M dupliquent 8M lignes). len(env) = consensus prompt-range —
# cutover-only.
OMI_TRAIN_SHARDS_ONLY = PROTOCOL_VERSION >= 4
```

2. Gater les valeurs existantes (mêmes lignes, même style upstream) :

```python
MAX_NEW_TOKENS_PROTOCOL_CAP = int(
    _os.environ.get(
        "RELIQUARY_MAX_NEW_TOKENS_PROTOCOL_CAP",
        "8192" if PROTOCOL_VERSION >= 4 else "16384",
    )
)
BFT_ENABLED = PROTOCOL_VERSION < 4
M_ROLLOUTS = 16 if PROTOCOL_VERSION >= 4 else 8
T_PROTO = 1.0 if PROTOCOL_VERSION >= 4 else 0.6
TOP_P_PROTO = 1.0 if PROTOCOL_VERSION >= 4 else 0.95
TOP_K_PROTO = 0 if PROTOCOL_VERSION >= 4 else 20
B_BATCH = 16 if PROTOCOL_VERSION >= 4 else 8
MAX_SUBMISSIONS_PER_HOTKEY_PER_WINDOW = (
    2 * B_BATCH if PROTOCOL_VERSION >= 4 else 8
)
MIN_EOS_PROBABILITY = 0.001 if PROTOCOL_VERSION >= 4 else 0.01
SAMPLING_MEDIAN_LOW_MAX = 0.05 if PROTOCOL_VERSION >= 4 else 0.30
SAMPLING_LOW_Q10_MAX = 0.0002 if PROTOCOL_VERSION >= 4 else 0.025
FORCED_SEED_ROLLOUT_FLOOR = 0.70 if PROTOCOL_VERSION >= 4 else 0.75
```

3. L'assert de cohérence BFT (~l.130) devient conditionnel :

```python
if BFT_ENABLED:
    assert MAX_NEW_TOKENS_PROTOCOL_CAP >= BFT_THINKING_BUDGET + BFT_ANSWER_BUDGET, (
        f"cap {MAX_NEW_TOKENS_PROTOCOL_CAP} < BFT {BFT_THINKING_BUDGET}+{BFT_ANSWER_BUDGET}"
    )
```

4. `FORCED_SEED_PROTOCOL_VERSION = protocol_version()` (~l.430) devient
   `FORCED_SEED_PROTOCOL_VERSION = PROTOCOL_VERSION` (même valeur, une seule
   source). Créer aussi `tests/v4helpers.py` (contenu en Global Constraints).

- [ ] **Step 4: Vérifier le pass + non-régression**

Run: `PYTHONPATH=. python3 -m pytest tests/test_v4_constants.py tests/test_bft_thinking_budget_env.py tests/test_envelope_v3_wire.py -q`
Expected: PASS (les 2 suites existantes prouvent que le défaut v3 n'a pas bougé).

- [ ] **Step 5: Commit**

```bash
git add reliquary/constants.py tests/v4helpers.py tests/test_v4_constants.py
git commit -m "feat(v4): constantes gatées RELIQUARY_PROTOCOL_VERSION=4, défaut v3 byte-identique"
```

---

### Task 2: `encode_prompt` raw-completion

**Files:**
- Modify: `reliquary/protocol/tokens.py` (fonction `encode_prompt`, ~l.42)
- Test: `tests/test_v4_raw_prompts.py`

**Interfaces:**
- Consumes: `constants.RAW_COMPLETION_PROMPTS` (Task 1).
- Produces: `encode_prompt(tokenizer, str) -> list[int]` — sous v4, encodage
  brut `add_special_tokens=False`, jamais de chat template.

- [ ] **Step 1: Test qui échoue**

```python
# tests/test_v4_raw_prompts.py
import pytest


@pytest.fixture(autouse=True)
def _restore_constants(monkeypatch):
    yield
    from tests.v4helpers import reload_constants
    reload_constants(monkeypatch)


class _ChatTokenizer:
    """Tokenizer factice qui DÉCLARE un chat template (piège Qwen3-4B-Base)."""
    chat_template = "{{ messages }} enable_thinking"

    def encode(self, text, add_special_tokens=True):
        assert add_special_tokens is False, "v4 doit passer add_special_tokens=False"
        return [len(text), 42]

    def apply_chat_template(self, messages, **kwargs):
        raise AssertionError("v4 ne doit JAMAIS appeler apply_chat_template")


def test_v4_encode_prompt_is_raw(monkeypatch):
    monkeypatch.setattr("reliquary.constants.RAW_COMPLETION_PROMPTS", True)
    from reliquary.protocol.tokens import encode_prompt
    assert encode_prompt(_ChatTokenizer(), "abc") == [3, 42]


def test_v3_encode_prompt_still_uses_template(monkeypatch):
    monkeypatch.setattr("reliquary.constants.RAW_COMPLETION_PROMPTS", False)
    from reliquary.protocol.tokens import encode_prompt

    class _Tok(_ChatTokenizer):
        def apply_chat_template(self, messages, **kwargs):
            return [1, 2, 3]

    assert encode_prompt(_Tok(), "abc") == [1, 2, 3]
```

- [ ] **Step 2: Vérifier l'échec**

Run: `PYTHONPATH=. python3 -m pytest tests/test_v4_raw_prompts.py -q`
Expected: FAIL — `apply_chat_template` appelé sous v4 (AssertionError).

- [ ] **Step 3: Implémentation (port byte-exact upstream)**

Dans `encode_prompt`, tout en tête de fonction :

```python
    # v4+ raw-completion : jamais de chat template, même si le tokenizer en
    # déclare un (un repo "-Base" livre le template famille sans l'avoir
    # appris). Import paresseux pour que les tests puissent monkeypatcher
    # reliquary.constants. Parité upstream 8c38992.
    from reliquary.constants import RAW_COMPLETION_PROMPTS

    if RAW_COMPLETION_PROMPTS:
        return list(tokenizer.encode(prompt_text, add_special_tokens=False))
```

- [ ] **Step 4: Vérifier le pass**

Run: `PYTHONPATH=. python3 -m pytest tests/test_v4_raw_prompts.py -q`
Expected: PASS (2 tests).

- [ ] **Step 5: Commit**

```bash
git add reliquary/protocol/tokens.py tests/test_v4_raw_prompts.py
git commit -m "feat(v4): encode_prompt raw-completion (jamais de chat template sous v4)"
```

---

### Task 3: Grader OMI v4 — ligne `Answer:` + délimiteurs LaTeX

**Files:**
- Modify: `reliquary/environment/openmathinstruct.py`
  (`_normalize_answer`, `_compute_omi_reward` ; ajouter `_ANSWER_LINE_RE`
  près de `_TEXT_RE`)
- Test: `tests/test_v4_omi_grader.py`

**Interfaces:**
- Consumes: `constants.RAW_COMPLETION_PROMPTS`.
- Produces: reward local identique au validateur v4 (sinon REWARD_MISMATCH).

- [ ] **Step 1: Tests qui échouent** (cas copiés de l'esprit des tests upstream)

```python
# tests/test_v4_omi_grader.py
import pytest
from reliquary.environment.openmathinstruct import _compute_omi_reward


@pytest.fixture(autouse=True)
def _restore_constants(monkeypatch):
    yield
    from tests.v4helpers import reload_constants
    reload_constants(monkeypatch)


def _reward(completion, gt="7", v4=True, monkeypatch=None):
    monkeypatch.setattr(
        "reliquary.constants.RAW_COMPLETION_PROMPTS", v4
    )
    return _compute_omi_reward(
        {"ground_truth": gt}, completion
    )


def test_v4_answer_line(monkeypatch):
    assert _reward("blah\nAnswer: 7\n", monkeypatch=monkeypatch) == 1.0


def test_v4_answer_line_markdown_emphasis(monkeypatch):
    assert _reward("**Answer:** 7", monkeypatch=monkeypatch) == 1.0


def test_v4_last_answer_line_wins(monkeypatch):
    assert _reward("Answer: 3\nAnswer: 7", monkeypatch=monkeypatch) == 1.0


def test_v4_inline_latex_delims_stripped(monkeypatch):
    assert _reward(r"Answer: \(7\)", monkeypatch=monkeypatch) == 1.0
    assert _reward(r"Answer: \[7\]", monkeypatch=monkeypatch) == 1.0


def test_v4_boxed_still_takes_precedence(monkeypatch):
    assert _reward(
        "Answer: 3\ndonc $\\boxed{7}$", monkeypatch=monkeypatch
    ) == 1.0


def test_v3_answer_line_ignored(monkeypatch):
    # v3 : la branche Answer: n'existe pas — reward par trailing number (3≠7).
    assert _reward("blah\nAnswer: 7\nfin 3", v4=False, monkeypatch=monkeypatch) == 0.0
```

- [ ] **Step 2: Vérifier l'échec**

Run: `PYTHONPATH=. python3 -m pytest tests/test_v4_omi_grader.py -q`
Expected: FAIL — les tests v4 `Answer:` retournent 0.0.

- [ ] **Step 3: Implémentation (port byte-exact upstream)**

Près de `_TEXT_RE` :

```python
# Ligne "Answer: X", tolérant l'emphase markdown autour du repère
# ("**Answer:** 0"). L'extraction boxed garde la priorité.
_ANSWER_LINE_RE = re.compile(r"^[\s*_]*Answer[\s*_]*:[\s*_]*(.+?)\s*$", re.MULTILINE)
```

Tout en tête de `_normalize_answer`, après `s = str(s)` :

```python
    # v4 raw-completion : les réponses arrivent en LaTeX inline/display
    # ("\(x\)" / "\[x\]") ; les délimiteurs ne portent jamais de sens. Gaté
    # pour que le grading v3 reste byte-identique. Import paresseux pour les
    # tests.
    from reliquary.constants import RAW_COMPLETION_PROMPTS
    if RAW_COMPLETION_PROMPTS:
        for delim in (r"\(", r"\)", r"\[", r"\]"):
            s = s.replace(delim, "")
```

Dans `_compute_omi_reward`, remplacer la branche `boxed is None` par la
version upstream (structure exacte du diff `8c38992`) :

```python
        if boxed is not None:
            candidate = _normalize_answer(_strip_boxed_wrapper(boxed))
        else:
            # v4 raw-completion : réponse via ligne "Answer:" ; v3 boxe, donc
            # branche v4-only pour garder le reward v3 byte-identique.
            from reliquary.constants import RAW_COMPLETION_PROMPTS
            answer_lines = (
                _ANSWER_LINE_RE.findall(completion)
                if RAW_COMPLETION_PROMPTS else []
            )
            if answer_lines:
                candidate = _normalize_answer(answer_lines[-1])
            else:
                tail = completion.strip().split("\n")[-1].strip()
                m = re.match(r"^([\-\+]?\d+(?:\.\d+)?(?:/\d+)?)", tail)
                if m is None:
                    return 0.0
                candidate = _normalize_answer(m.group(1))
```

- [ ] **Step 4: Vérifier le pass + non-régression grader**

Run: `PYTHONPATH=. python3 -m pytest tests/test_v4_omi_grader.py -q && PYTHONPATH=. python3 -m pytest tests/ -q -k "omi or grader or reward" 2>&1 | tail -3`
Expected: PASS partout (aucune régression des tests grader existants).

- [ ] **Step 5: Commit**

```bash
git add reliquary/environment/openmathinstruct.py tests/test_v4_omi_grader.py
git commit -m "feat(v4): grader OMI — ligne Answer: + strip délimiteurs LaTeX (gaté raw-completion)"
```

---

### Task 4: Filtre shards OMI `train-*` (consensus prompt-range)

**Files:**
- Modify: `reliquary/environment/virtual_parquet.py` (`__init__`,
  `_local_manifest_files`, listing distant ~l.289)
- Modify: `reliquary/environment/openmathinstruct.py` (`_load_dataset`)
- Test: `tests/test_v4_omi_shards.py`

**Interfaces:**
- Consumes: `constants.OMI_TRAIN_SHARDS_ONLY`.
- Produces: `VirtualParquetDataset(..., filename_prefix: str | None = None)` ;
  `_load_dataset` passe `filename_prefix="train-"` sous v4 et REFUSE les
  snapshots `save_to_disk` locaux (RuntimeError).

⚠️ C'est LE changement consensus : un `len(env)` divergent = 100 %
PROMPT_MISMATCH silencieux. Le filtre doit s'appliquer aux DEUX chemins de
listing (cache local + fs distant), comme upstream.

- [ ] **Step 1: Tests qui échouent**

```python
# tests/test_v4_omi_shards.py
import pytest
from reliquary.environment.virtual_parquet import VirtualParquetDataset


@pytest.fixture(autouse=True)
def _restore_constants(monkeypatch):
    yield
    from tests.v4helpers import reload_constants
    reload_constants(monkeypatch)


def _vp(prefix):
    return VirtualParquetDataset(
        "owner/repo", "rev", columns=["problem"], filename_prefix=prefix,
    )


def test_shard_included_prefix_filters_basename():
    vp = _vp("train-")
    assert vp._shard_included("data/train-00001-of-00500.parquet")
    assert not vp._shard_included("data/train_1M-00001.parquet")
    assert not vp._shard_included("data/train_5M-00001.parquet")
    # le préfixe s'applique au BASENAME, pas au chemin
    assert vp._shard_included("train-huggingface/data/train-00001.parquet")


def test_shard_included_none_accepts_everything():
    vp = _vp(None)
    assert vp._shard_included("data/train_1M-00001.parquet")


def test_v4_load_dataset_refuses_local_snapshot(tmp_path, monkeypatch):
    monkeypatch.setattr("reliquary.constants.OMI_TRAIN_SHARDS_ONLY", True)
    (tmp_path / "dataset_info.json").write_text("{}")
    from reliquary.environment.openmathinstruct import _load_dataset
    with pytest.raises(RuntimeError, match="train- shard filter"):
        _load_dataset(str(tmp_path), "rev")


def test_v4_load_dataset_passes_prefix(monkeypatch):
    monkeypatch.setattr("reliquary.constants.OMI_TRAIN_SHARDS_ONLY", True)
    captured = {}

    class _FakeVP:
        def __init__(self, repo, revision, **kwargs):
            captured.update(kwargs)

    from reliquary.environment import virtual_parquet as vp_mod
    monkeypatch.setattr(vp_mod, "VirtualParquetDataset", _FakeVP)
    from reliquary.environment.openmathinstruct import _load_dataset
    _load_dataset("owner/repo", "rev")
    assert captured["filename_prefix"] == "train-"


def test_v3_load_dataset_no_prefix(monkeypatch):
    monkeypatch.setattr("reliquary.constants.OMI_TRAIN_SHARDS_ONLY", False)
    captured = {}

    class _FakeVP:
        def __init__(self, repo, revision, **kwargs):
            captured.update(kwargs)

    from reliquary.environment import virtual_parquet as vp_mod
    monkeypatch.setattr(vp_mod, "VirtualParquetDataset", _FakeVP)
    from reliquary.environment.openmathinstruct import _load_dataset
    _load_dataset("owner/repo", "rev")
    assert captured["filename_prefix"] is None
```

- [ ] **Step 2: Vérifier l'échec**

Run: `PYTHONPATH=. python3 -m pytest tests/test_v4_omi_shards.py -q`
Expected: FAIL — TypeError `filename_prefix` inattendu.

- [ ] **Step 3: Implémentation (port byte-exact upstream, 3 morceaux)**

`virtual_parquet.py` — `__init__` : ajouter le paramètre keyword
`filename_prefix: Optional[str] = None` et

```python
        # Si posé, seuls les parquet dont le BASENAME commence par ce préfixe
        # entrent dans le manifest. Appliqué aux DEUX chemins de listing :
        # len() est le consensus prompt-range, un filtre sur un seul chemin le
        # forkerait au fallback cache.
        self._filename_prefix = filename_prefix
```

Méthode :

```python
    def _shard_included(self, path: str) -> bool:
        if self._filename_prefix is None:
            return True
        name = str(path).replace("\\", "/").rsplit("/", 1)[-1]
        return name.startswith(self._filename_prefix)
```

`_local_manifest_files` : `allow_patterns=[f"{self._data_dir}/{self._filename_prefix or ''}*.parquet"]`
et le glob filtré `if path.is_file() and self._shard_included(str(path))`.
Listing distant (~l.289) : `if str(p).endswith(".parquet") and self._shard_included(str(p))`.

`openmathinstruct.py` — `_load_dataset` : port exact du diff upstream
(import paresseux `OMI_TRAIN_SHARDS_ONLY` en tête ; RuntimeError sur snapshot
local sous v4 avec le message contenant « train- shard filter » ; import
`from reliquary.environment import virtual_parquet as _vp` et appel
`_vp.VirtualParquetDataset(repo, revision, columns=["problem", "expected_answer"],
filename_prefix="train-" if OMI_TRAIN_SHARDS_ONLY else None)` — l'appel via le
module est ce qui rend le monkeypatch du test possible).

- [ ] **Step 4: Vérifier le pass + non-régression**

Run: `PYTHONPATH=. python3 -m pytest tests/test_v4_omi_shards.py -q && PYTHONPATH=. python3 -m pytest tests/ -q -k "parquet or prompt_range" 2>&1 | tail -3`
Expected: PASS.

- [ ] **Step 5: Commit**

```bash
git add reliquary/environment/virtual_parquet.py reliquary/environment/openmathinstruct.py tests/test_v4_omi_shards.py
git commit -m "feat(v4): manifest OMI train-* seulement (filename_prefix, consensus len(env))"
```

---

### Task 5: Zone gate v4 (σ 0.24/0.22, k∈[1,15] payable)

**Files:**
- Modify: `reliquary/miner/zone.py`
- Test: `tests/test_v4_zone.py`

**Interfaces:**
- Consumes: `constants.PROTOCOL_VERSION`.
- Produces: `zone.active_thresholds() -> tuple[float, float]` (steady,
  bootstrap) ; `is_in_zone` inchangé de signature.

- [ ] **Step 1: Tests qui échouent**

```python
# tests/test_v4_zone.py
import math
import pytest
from reliquary.miner import zone


@pytest.fixture(autouse=True)
def _restore_constants(monkeypatch):
    yield
    from tests.v4helpers import reload_constants
    reload_constants(monkeypatch)


def _sigma(k, m):
    p = k / m
    return math.sqrt(p * (1 - p))


def test_v4_thresholds(monkeypatch):
    monkeypatch.setattr("reliquary.constants.PROTOCOL_VERSION", 4)
    assert zone.active_thresholds() == (0.24, 0.22)


def test_v3_thresholds_unchanged(monkeypatch):
    monkeypatch.setattr("reliquary.constants.PROTOCOL_VERSION", 3)
    assert zone.active_thresholds() == (0.43, 0.33)


def test_v4_k1_of_16_in_zone(monkeypatch):
    monkeypatch.setattr("reliquary.constants.PROTOCOL_VERSION", 4)
    # σ(k=1, M=16) = 0.2421 > 0.24 → payable sous v4
    rewards = [1.0] + [0.0] * 15
    assert zone.is_in_zone(rewards, bootstrap=False)


def test_v4_unanime_out_of_zone(monkeypatch):
    monkeypatch.setattr("reliquary.constants.PROTOCOL_VERSION", 4)
    assert not zone.is_in_zone([0.0] * 16, bootstrap=False)
    assert not zone.is_in_zone([1.0] * 16, bootstrap=False)


def test_v3_k1_of_8_still_rejected(monkeypatch):
    monkeypatch.setattr("reliquary.constants.PROTOCOL_VERSION", 3)
    rewards = [1.0] + [0.0] * 7  # σ = 0.331 < 0.43
    assert not zone.is_in_zone(rewards, bootstrap=False)
```

⚠️ Adapter les appels à la VRAIE signature de `zone.is_in_zone` (la lire
d'abord — elle prend peut-être une σ déjà calculée ou un vecteur ; garder la
sémantique du test, pas sa forme).

- [ ] **Step 2: Vérifier l'échec**

Run: `PYTHONPATH=. python3 -m pytest tests/test_v4_zone.py -q`
Expected: FAIL — `active_thresholds` absent.

- [ ] **Step 3: Implémentation**

`zone.py` : remplacer les constantes en dur par

```python
ZONE_THRESHOLD_STEADY = 0.43     # v3 — gardées pour compat imports existants
ZONE_THRESHOLD_BOOTSTRAP = 0.33


def active_thresholds() -> tuple[float, float]:
    """(steady, bootstrap) du protocole actif. v4 = critère dynamic-sampling
    DAPO : σ(k=1|15, M=16)=0.2421 → 0.24 admet tout k∈[1,15] ; 0.22 bootstrap.
    Import paresseux pour la testabilité (pattern upstream)."""
    from reliquary.constants import PROTOCOL_VERSION
    if PROTOCOL_VERSION >= 4:
        return 0.24, 0.22
    return ZONE_THRESHOLD_STEADY, ZONE_THRESHOLD_BOOTSTRAP
```

et faire lire `is_in_zone` via `steady, boot = active_thresholds()` au lieu
des constantes module. Vérifier les AUTRES lecteurs des constantes zone
(`grep -rn "ZONE_THRESHOLD" reliquary/ | grep -v test`) et les basculer sur
`active_thresholds()` s'ils gatent une décision d'éligibilité.

- [ ] **Step 4: Vérifier le pass + tests zone existants**

Run: `PYTHONPATH=. python3 -m pytest tests/test_v4_zone.py -q && PYTHONPATH=. python3 -m pytest tests/ -q -k "zone or out_of_zone" 2>&1 | tail -3`
Expected: PASS.

- [ ] **Step 5: Commit**

```bash
git add reliquary/miner/zone.py tests/test_v4_zone.py
git commit -m "feat(v4): zone gate 0.24/0.22 — tout k∈[1,15] payable sous v4"
```

---

### Task 6: BFT éteint sous v4 — jamais de claim `forced`

**Files:**
- Modify: aucun attendu (le gate existe : `reliquary/miner/bft.py` l.16-18 lit
  `BFT_ENABLED`) — tâche de VERROUILLAGE par tests
- Test: `tests/test_v4_no_bft.py`

**Interfaces:**
- Consumes: `constants.BFT_ENABLED` (False sous v4, Task 1),
  `bft.bft_enabled_for_env`.
- Produces: garantie testée : sous v4 aucun rollout n'a `forced=True` ni
  `force_span` (le validateur v4 fail-close toute claim forced —
  `validate_force_span` upstream).

- [ ] **Step 1: Écrire les tests**

```python
# tests/test_v4_no_bft.py
import pytest


@pytest.fixture(autouse=True)
def _restore_constants(monkeypatch):
    yield
    from tests.v4helpers import reload_constants
    reload_constants(monkeypatch)


def test_v4_bft_disabled_for_math(monkeypatch):
    monkeypatch.setattr("reliquary.constants.BFT_ENABLED", False)
    from reliquary.miner import bft
    assert not bft.bft_enabled_for_env("openmathinstruct")
    assert not bft.bft_enabled_for_env("opencodeinstruct")
    assert not bft.bft_enabled_for_env(None)


def test_v3_bft_still_on_for_math(monkeypatch):
    monkeypatch.setattr("reliquary.constants.BFT_ENABLED", True)
    from reliquary.miner import bft
    assert bft.bft_enabled_for_env("openmathinstruct")
    assert not bft.bft_enabled_for_env("opencodeinstruct")
```

⚠️ Étape d'audit obligatoire AVANT de conclure la tâche : `grep -n
"forced\|force_span" reliquary/miner/engine.py | grep -v '\.bak'` et vérifier
que CHAQUE écriture de `forced=True`/`force_span` est derrière
`bft_enabled_for_env(...)` (ou dans `bft.py`, lui-même gaté). Si un chemin
écrit `forced` sans gate → le corriger dans CETTE tâche et ajouter un test.
Leçon 2026-08-05 : vérifier le chemin RÉEL, pas l'existence de la garde.

- [ ] **Step 2: Lancer** — `PYTHONPATH=. python3 -m pytest tests/test_v4_no_bft.py -q`
Expected: PASS direct si le gate `bft.py` lit bien `BFT_ENABLED` par import
paresseux — SINON (import module-level figé) le test échoue : convertir
`bft.py` à l'import paresseux dans la fonction et re-lancer.

- [ ] **Step 3: Commit**

```bash
git add tests/test_v4_no_bft.py reliquary/miner/bft.py
git commit -m "test(v4): BFT éteint sous v4 — aucun chemin n'émet forced/force_span"
```

---

### Task 7: Wire v4 — 16 rollouts, domaine v4, version émise

**Files:**
- Modify: `reliquary/protocol/submission.py` SEULEMENT si le test révèle un
  import module-level figé (M_ROLLOUTS lu à l'import — reload requis dans le
  test, pas de changement de code attendu)
- Test: `tests/test_v4_wire.py`

**Interfaces:**
- Consumes: constantes Task 1.
- Produces: garantie testée : sous env `RELIQUARY_PROTOCOL_VERSION=4`, le
  modèle de soumission exige 16 rollouts et les constantes wire émettent 4 /
  domaine v4.

- [ ] **Step 1: Tests**

```python
# tests/test_v4_wire.py
import importlib
import pytest
from tests.v4helpers import reload_constants


@pytest.fixture(autouse=True)
def _restore(monkeypatch):
    yield
    reload_constants(monkeypatch)
    import reliquary.protocol.submission as s
    importlib.reload(s)


def test_v4_submission_requires_16_rollouts(monkeypatch):
    reload_constants(monkeypatch, 4)
    import reliquary.protocol.submission as s
    importlib.reload(s)
    assert s.M_ROLLOUTS == 16


def test_v4_wire_identity(monkeypatch):
    c = reload_constants(monkeypatch, 4)
    assert c.FORCED_SEED_PROTOCOL_VERSION == 4
    assert c.FORCED_SEED_DOMAIN == "reliquary-forced-seed-v4"
    assert c.MAX_SUBMISSIONS_PER_HOTKEY_PER_WINDOW == 32
```

- [ ] **Step 2: Lancer** — `PYTHONPATH=. python3 -m pytest tests/test_v4_wire.py -q`
Expected: PASS si Task 1 est complet (sinon corriger ce que le test révèle —
p.ex. un `M_ROLLOUTS` recopié en littéral quelque part).

- [ ] **Step 3: Commit**

```bash
git add tests/test_v4_wire.py
git commit -m "test(v4): identité wire — 16 rollouts, domaine forced-seed v4, cap 32"
```

---

### Task 8: Script de lancement v4 + suite complète + docs

**Files:**
- Create: `ops/start_miner_v4_DRAFT.sh`
- Modify: `docs/superpowers/plans/2026-08-17-port-v4-dapo.md` (cocher) —
  la MAJ de CLAUDE.md se fait dans l'arbre principal, PAS dans le worktree

- [ ] **Step 1: Écrire le draft de launcher** (NE PAS lancer — le validateur
  live est v3 ; ce script ne sert qu'au jour de la bascule)

```bash
#!/usr/bin/env bash
# DRAFT v4 — NE PAS LANCER tant que /health du validateur n'annonce pas
# protocol_version 4 / generation_profile_id qwen3-4b-base-dapo-v4.
# Bascule : RELIQUARY_PROTOCOL_VERSION=4 fait TOUT (constantes, domaine
# forced-seed, BFT off, cap 8192, 16 rollouts, sampling 1.0/1.0/0).
set -euo pipefail

export RELIQUARY_PROTOCOL_VERSION=4
# Le checkpoint réel est suivi via /state ; le modèle de départ v4 :
CHECKPOINT="${CHECKPOINT:-Qwen/Qwen3-4B-Base}"
# Sanity avant lancement (les 3 doivent afficher les valeurs v4) :
PYTHONPATH=. python3 - <<'EOF'
from reliquary import constants as c
assert c.PROTOCOL_VERSION == 4 and c.M_ROLLOUTS == 16 and not c.BFT_ENABLED
assert c.MAX_NEW_TOKENS_PROTOCOL_CAP == 8192 and c.TOP_K_PROTO == 0
print("v4 constants OK:", c.FORCED_SEED_DOMAIN)
EOF

cd /root/reliquary-miner-priv && PYTHONPATH=. \
  python3 -m reliquary.cli.main mine \
    --wallet-name camille81-v2 --hotkey hotkey81 --network finney --netuid 81 \
    --checkpoint "$CHECKPOINT" \
    --log-level INFO 2>&1 | tee miner_v4.log
```

- [ ] **Step 2: Suite complète de la branche**

Run: `PYTHONPATH=. python3 -m pytest tests/ -q 2>&1 | tail -5`
Expected: mêmes 7 fails pré-existants que la baseline (fixtures v2), ZÉRO fail
nouveau, tous les tests v4 verts.

- [ ] **Step 3: Commit**

```bash
git add ops/start_miner_v4_DRAFT.sh
git commit -m "ops(v4): draft launcher jour-J (flag unique RELIQUARY_PROTOCOL_VERSION=4)"
```

---

## Hors périmètre de cette branche (jour J, GPU requis)

1. **Gate forced-seed 4B-Base sur GPU** : re-passer
   `ops/validate_vllm_forced_seed_group.py` avec le flag v4 sur la box
   (support complet T=1.0 — planchers 0.80 groupe / 0.70 rollout).
2. **Perf** : à top_k=0/top_p=1.0 le tri top_p du processeur FS disparaît →
   re-bencher ; M=16 double la génération/groupe, cap 8192 la divise par 2.
3. **Prédicteur** : v4.x obsolète le jour J (labels v3, modèle thinking).
   Redémarrer la collecte SAMPLE_DUMP dès la bascule ; quasi tout k∈[1,15]
   devient payable → la cible du prédicteur devient le RANG d'enchère
   `std·(1-mean)^δ`, plus la zone.
4. **Réconciliation OOM fixes** : l'arbre prod local a
   `_kill_stale_engine_cores` non commité (+ tests + 3 scripts ops non
   trackés) — à commiter côté prod puis merger dans cette branche AVANT tout
   déploiement v4.
5. MAJ CLAUDE.md + mémoires au moment du merge de la branche.
